{{#each Learnnew}}
<div class="row newssubtitle">
    <div class="col-md-6 learntext">
        <a class="morecolor" target="_blank" href="/newsContent?news_id={{{news_id}}}">{{news_name}}</a>
    </div>
    <div class="col-md-6">
        <p class="timeacho">{{update_time}}</p>
    </div>
</div>
{{/each}}